#include <catch2/catch.hpp>
#include <vector>
using namespace std;

void findmaxOfK(vector<int>& ip, int k);

TEST_CASE( "2: Factorial of 0 is 1 (fail)", "[multi-file:2]" ) {
    // REQUIRE( findmaxOfK(ip, 3));
    REQUIRE(true);
}